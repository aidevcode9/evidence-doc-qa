# Base URL for the API used by the Next.js frontend.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Allowed API host for SSRF protection in document proxy (production only)
# Set to your Container Apps hostname, e.g., "your-app.azurecontainerapps.io"
ALLOWED_API_HOST=
